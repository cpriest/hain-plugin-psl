'use strict';
let { log, indent } = require('./utils.js');

module.exports = (
	class Pattern {
		/**
		 * @constructor
		 * @param {PatternDefinition} def
		 */
		constructor(def) {
			this.def = def;
		}
	}
);
